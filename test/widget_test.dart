import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App builds without errors', (WidgetTester tester) async {
    // Placeholder test — replace with real integration tests once API is connected
    expect(true, isTrue);
  });
}
