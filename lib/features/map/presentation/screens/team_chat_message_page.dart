import 'package:flutter/material.dart';
class TeamChatListPage extends StatelessWidget {
  @override Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: Text('Chat')), body: Center(child: Text('Chat not ported yet')));
}
class TeamChatMessagePage extends StatelessWidget {
  @override Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: Text('Chat')), body: Center(child: Text('Chat not ported yet')));
}
